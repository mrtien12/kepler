import { Button, ScrollArea, Card } from "@mantine/core";
import {Budget} from "@/types/budget";

interface BudgetListProps {
    budgets: Budget[]
}


export default function BudgetList({budgets}: BudgetListProps) {

    
}